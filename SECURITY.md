# Security & Responsible Use
- Store secrets only in environment variables. Do not commit `.env` or keys.
- Respect API rate limits and platform ToS.
- Label crowd-sourced data clearly; verify against trusted domains/directories when possible (e.g., Feeding America members, 211).
- If you enable public reads on `submissions`, restrict to `verified=true`.

